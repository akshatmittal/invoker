---
"@akshatmittal/invoker": minor
---

Add an optional Slack reporter with a combined run message, per-Workflow metadata cards, Task result tables, a shared localized footer, and styled failure cards grouped by Task.
